import{Link2}from"lucide-react";import{LineLinkForm}from"@/components/line/line-link-form";import{lineMockMode}from"@/lib/env";
export default async function LineLinkPage({searchParams}:{searchParams:Promise<{code?:string}>}){const code=(await searchParams).code??"";return <main className="grid min-h-screen place-items-center p-5"><section className="w-full max-w-md rounded-2xl border bg-[var(--surface)] p-6 shadow-sm"><div className="mb-6 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-[#06C755] text-white"><Link2/></span><h1 className="mt-3 text-xl font-bold">เชื่อมบัญชีผู้เช่า</h1><p className="text-sm text-slate-500">เชื่อม LINE กับข้อมูลผู้เช่าของหอพักอย่างปลอดภัย</p></div>{code.length===32?<LineLinkForm code={code} liffId={process.env.NEXT_PUBLIC_LINE_LIFF_ID??""} mockMode={lineMockMode()}/>:<p className="rounded-lg bg-red-50 p-3 text-center text-sm text-red-700">ลิงก์ไม่ถูกต้อง กรุณาขอลิงก์ใหม่จากเจ้าหน้าที่</p>}</section></main>}
