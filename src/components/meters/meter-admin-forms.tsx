"use client";
import{useActionState,useEffect}from"react";import{Gauge,LoaderCircle,ReceiptText}from"lucide-react";import{useRouter}from"next/navigation";import{createMeterAction,createRatePlanAction}from"@/app/meters/actions";import type{MeterActionState}from"@/features/meters/schemas";
const field="mt-1.5 w-full rounded-lg border border-slate-300 bg-transparent px-3 py-2.5";
export function MeterAdminForms({rooms,today}:{rooms:{id:string;label:string}[];today:string}){const router=useRouter();const[meterState,meterAction,meterPending]=useActionState<MeterActionState,FormData>(createMeterAction,{});const[rateState,rateAction,ratePending]=useActionState<MeterActionState,FormData>(createRatePlanAction,{});useEffect(()=>{if(meterState.success||rateState.success)router.refresh();},[meterState.success,rateState.success,router]);return <div className="grid items-start gap-5 xl:grid-cols-2"><form action={meterAction} className="space-y-3 rounded-xl border border-slate-200 bg-[var(--surface)] p-5"><div className="flex items-center gap-2"><Gauge className="text-teal-700"/><div><h2 className="font-bold">เพิ่มมิเตอร์ให้ห้อง</h2><p className="text-xs text-[var(--muted)]">รองรับน้ำ ไฟ มิเตอร์เปลี่ยนใหม่ และมิเตอร์วนรอบ</p></div></div><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm">ห้อง<select name="roomId" required className={field}><option value="">เลือกห้อง</option>{rooms.map(r=><option key={r.id} value={r.id}>{r.label}</option>)}</select></label><label className="text-sm">ประเภท<select name="meterType" className={field}><option value="water">น้ำ</option><option value="electricity">ไฟฟ้า</option><option value="other">อื่น ๆ</option></select></label></div><div className="grid gap-3 sm:grid-cols-3"><label className="text-sm">หมายเลขมิเตอร์<input name="meterNumber" required className={field}/></label><label className="text-sm">ค่าเริ่มต้น<input name="initialReading" defaultValue="0.000" inputMode="decimal" required className={field}/></label><label className="text-sm">ค่าสูงสุดก่อนวนรอบ<input name="rolloverMax" placeholder="เช่น 99999" inputMode="decimal" className={field}/></label></div><label className="block text-sm">หมายเหตุ<textarea name="notes" rows={2} className={field}/></label>{meterState.error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{meterState.error}</p>}{meterState.success&&<p className="rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{meterState.message}</p>}<button disabled={meterPending} className="flex items-center gap-2 rounded-lg bg-teal-700 px-4 py-2.5 font-semibold text-white disabled:opacity-50">{meterPending&&<LoaderCircle size={18} className="animate-spin"/>}บันทึกมิเตอร์</button></form><form action={rateAction} className="space-y-3 rounded-xl border border-slate-200 bg-[var(--surface)] p-5"><div className="flex items-center gap-2"><ReceiptText className="text-blue-700"/><div><h2 className="font-bold">เพิ่มอัตราค่าน้ำ/ไฟ</h2><p className="text-xs text-[var(--muted)]">อัตราใหม่ไม่มีผลย้อนหลังกับรายการที่บันทึกแล้ว</p></div></div><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm">ชื่ออัตรา<input name="name" required placeholder="เช่น ค่าไฟปี 2569" className={field}/></label><label className="text-sm">ประเภทมิเตอร์<select name="meterType" className={field}><option value="electricity">ไฟฟ้า</option><option value="water">น้ำ</option><option value="other">อื่น ๆ</option></select></label></div><div className="grid gap-3 sm:grid-cols-3"><label className="text-sm">รูปแบบ<select name="pricingType" className={field}><option value="fixed">ราคาคงที่</option><option value="tiered">ขั้นบันได</option></select></label><label className="text-sm">ราคาต่อหน่วย<input name="pricePerUnit" defaultValue="8.0000" inputMode="decimal" className={field}/></label><label className="text-sm">ค่าขั้นต่ำ<input name="minimumCharge" defaultValue="0.00" inputMode="decimal" required className={field}/></label></div><label className="block text-sm">วันที่เริ่มใช้<input name="effectiveFrom" type="date" defaultValue={today} required className={field}/></label><label className="block text-sm">ขั้นบันได (JSON — ใช้เมื่อเลือกรูปแบบขั้นบันได)<textarea name="tiersJson" rows={3} defaultValue={'[{"fromUnit":"0","toUnit":"10","pricePerUnit":"5"},{"fromUnit":"10","toUnit":null,"pricePerUnit":"8"}]'} className={`${field} font-mono text-xs`}/></label>{rateState.fieldErrors?.tiersJson?.[0]&&<p className="text-sm text-red-700">{rateState.fieldErrors.tiersJson[0]}</p>}{rateState.error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{rateState.error}</p>}{rateState.success&&<p className="rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{rateState.message}</p>}<button disabled={ratePending} className="flex items-center gap-2 rounded-lg bg-blue-700 px-4 py-2.5 font-semibold text-white disabled:opacity-50">{ratePending&&<LoaderCircle size={18} className="animate-spin"/>}บันทึกอัตราใหม่</button></form></div>}
